---
title: Command Line
layout: docs
toc_level: 2
---

# Command Line

To list available commands, run `flynn help`.

<% docs.each do |title, contents| %>
## <%= title %>
<% usage, description, sections = parse contents %>
<% description.each do |par| %>
<%= par + "\n" %>
<% end %>
<pre><%= h usage.sub("usage: ", "       ").strip_heredoc %></pre>
<% sections.each do |section| %>
### <%= h section[:title] %>
<%= preprocess(section) + "\n\n" %>
<% end %>
<% end %>

