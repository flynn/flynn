include_rules
: |> !go |> flynn-controller-examples

: img/files.sh flynn-controller-examples |> !build-layer |> layer/files.json
: layer/files.json | $(ROOT)/<builder> |> ^ build-artifact controller-examples^ $(ROOT)/builder/bin/flynn-build artifact ubuntu-trusty %f > %o |> $(ROOT)/image/controller-examples.json
