include_rules
: |> !go |> flynn-release
: flynn-release | $(ROOT)/bootstrap/<manifest> |> ^ images.json^ cat $(ROOT)/log/docker-layer1.log > /dev/null && sudo ./flynn-release images --output=%o --image-url-prefix=@(IMAGE_URL_PREFIX) --checksums=@(IMAGE_CHECKSUMS) |> $(ROOT)/images.json
