include_rules
: |> !go |> flynn-release
: flynn-release | $(ROOT)/bootstrap/<manifest> |> ^ version.json^ cat $(ROOT)/image/bootstrapped.json > /dev/null && ./flynn-release manifest --output=%o --image-dir=$(ROOT)/image/bootstrapped --image-repository=@(IMAGE_REPOSITORY) version_template.json |> $(ROOT)/version.json
