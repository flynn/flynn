export GOPATH

ROOT = $(TUP_CWD)

!go = |> go build -o %o |>
!docker = |> docker build -t flynn/%d . |>
!cp = |> cp %f %o |>
!manifest = | $(ROOT)/<version> |> $(ROOT)/util/release/flynn-release manifest --output=%o --id-file=$(ROOT)/version.json manifest_template.json |> bin/manifest.json
